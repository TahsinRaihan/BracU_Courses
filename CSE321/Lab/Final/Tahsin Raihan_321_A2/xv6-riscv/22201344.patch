diff --git a/Makefile b/Makefile
index b262c0a..5a2302d 100644
--- a/Makefile
+++ b/Makefile
@@ -145,7 +145,7 @@ UPROGS=\
 	$U/_logstress\
 	$U/_forphan\
 	$U/_dorphan\
-
+	$U/_test_scheduler
 fs.img: mkfs/mkfs README $(UPROGS)
 	mkfs/mkfs fs.img README $(UPROGS)
 
@@ -166,7 +166,7 @@ QEMUGDB = $(shell if $(QEMU) -help | grep -q '^-gdb'; \
 	then echo "-gdb tcp::$(GDBPORT)"; \
 	else echo "-s -p $(GDBPORT)"; fi)
 ifndef CPUS
-CPUS := 3
+CPUS := 1
 endif
 
 QEMUOPTS = -machine virt -bios none -kernel $K/kernel -m 128M -smp $(CPUS) -nographic
diff --git a/kernel/proc.c b/kernel/proc.c
index 22a5401..97efb93 100644
--- a/kernel/proc.c
+++ b/kernel/proc.c
@@ -7,6 +7,28 @@
 #include "defs.h"
 
 struct cpu cpus[NCPU];
+unsigned long rand_next = 1;
+
+int
+do_rand(unsigned long *ctx)
+{
+  long hi, lo, x;
+  x = (*ctx % 0x7ffffffe) + 1;
+  hi = x / 127773;
+  lo = x % 127773;
+  x = 16807 * lo - 2836 * hi;
+  if (x < 0)
+    x += 0x7fffffff;
+  x--;
+  *ctx = x;
+  return (x);
+}
+
+int
+rand(void)
+{
+  return (do_rand(&rand_next));
+}
 
 struct proc proc[NPROC];
 
@@ -124,7 +146,8 @@ allocproc(void)
 found:
   p->pid = allocpid();
   p->state = USED;
-
+  p->tickets = 1;
+  p->rounds = 0;
   // Allocate a trapframe page.
   if((p->trapframe = (struct trapframe *)kalloc()) == 0){
     freeproc(p);
@@ -227,6 +250,8 @@ userinit(void)
   p->cwd = namei("/");
 
   p->state = RUNNABLE;
+  p->tickets = 10;
+  p->rounds = 0;
 
   release(&p->lock);
 }
@@ -299,6 +324,8 @@ kfork(void)
   release(&wait_lock);
 
   acquire(&np->lock);
+  np->tickets = p->tickets;
+  np->rounds = 0;
   np->state = RUNNABLE;
   release(&np->lock);
 
@@ -416,7 +443,7 @@ kwait(uint64 addr)
 
 // Per-CPU process scheduler.
 // Each CPU calls scheduler() after setting itself up.
-// Scheduler never returns.  It loops, doing:
+// Schduler never returns.  It loops, doing:
 //  - choose a process to run.
 //  - swtch to start running that process.
 //  - eventually that process transfers control
@@ -426,42 +453,56 @@ scheduler(void)
 {
   struct proc *p;
   struct cpu *c = mycpu();
+  int total_tickets;
+  int current_ticket_count;
+  long winning_ticket;
 
   c->proc = 0;
   for(;;){
-    // The most recent process to run may have had interrupts
-    // turned off; enable them to avoid a deadlock if all
-    // processes are waiting. Then turn them back off
-    // to avoid a possible race between an interrupt
-    // and wfi.
+    // Avoid deadlock by enabling interrupts.
     intr_on();
-    intr_off();
+    intr_off(); 
 
-    int found = 0;
+    total_tickets = 0;
     for(p = proc; p < &proc[NPROC]; p++) {
       acquire(&p->lock);
       if(p->state == RUNNABLE) {
-        // Switch to chosen process.  It is the process's job
-        // to release its lock and then reacquire it
-        // before jumping back to us.
-        p->state = RUNNING;
-        c->proc = p;
-        swtch(&c->context, &p->context);
-
-        // Process is done running for now.
-        // It should have changed its p->state before coming back.
-        c->proc = 0;
-        found = 1;
+        total_tickets += p->tickets;
       }
       release(&p->lock);
     }
-    if(found == 0) {
-      // nothing to run; stop running on this core until an interrupt.
+
+    if (total_tickets > 0) {
+      winning_ticket = rand() % total_tickets;
+      current_ticket_count = 0;
+
+      for(p = proc; p < &proc[NPROC]; p++) {
+        acquire(&p->lock);
+        if(p->state == RUNNABLE) {
+          current_ticket_count += p->tickets;
+          if (current_ticket_count > winning_ticket) {
+            p->state = RUNNING;
+            p->rounds++;
+            c->proc = p;
+            swtch(&c->context, &p->context);
+            c->proc = 0;
+            release(&p->lock);
+            break; 
+          }
+        }
+        release(&p->lock);
+      }
+    } else {
+      intr_on();
       asm volatile("wfi");
     }
   }
 }
-
+    // The most recent process to run may have had interrupts
+    // turned off; enable them to avoid a deadlock if all
+    // processes are waiting. Then turn them back off
+    // to avoid a possible race between an interrupt
+    // and wfi.
 // Switch to scheduler.  Must hold only p->lock
 // and have changed proc->state. Saves and restores
 // intena because intena is a property of this
@@ -684,7 +725,7 @@ procdump(void)
       state = states[p->state];
     else
       state = "???";
-    printf("%d %s %s", p->pid, state, p->name);
+    printf("%d %s %s %d %d", p->pid, state, p->name, p->tickets, p->rounds);
     printf("\n");
   }
 }
diff --git a/kernel/proc.h b/kernel/proc.h
index d021857..72d47c2 100644
--- a/kernel/proc.h
+++ b/kernel/proc.h
@@ -103,5 +103,7 @@ struct proc {
   struct context context;      // swtch() here to run process
   struct file *ofile[NOFILE];  // Open files
   struct inode *cwd;           // Current directory
-  char name[16];               // Process name (debugging)
+  char name[16];
+  int tickets;
+  int rounds;               // Process name (debugging)
 };
diff --git a/kernel/syscall.c b/kernel/syscall.c
index 076d965..a44c514 100644
--- a/kernel/syscall.c
+++ b/kernel/syscall.c
@@ -101,7 +101,7 @@ extern uint64 sys_unlink(void);
 extern uint64 sys_link(void);
 extern uint64 sys_mkdir(void);
 extern uint64 sys_close(void);
-
+extern uint64 sys_settickets(void);
 // An array mapping syscall numbers from syscall.h
 // to the function that handles the system call.
 static uint64 (*syscalls[])(void) = {
@@ -126,6 +126,7 @@ static uint64 (*syscalls[])(void) = {
 [SYS_link]    sys_link,
 [SYS_mkdir]   sys_mkdir,
 [SYS_close]   sys_close,
+[SYS_settickets]   sys_settickets,
 };
 
 void
diff --git a/kernel/syscall.h b/kernel/syscall.h
index 3dd926d..055f698 100644
--- a/kernel/syscall.h
+++ b/kernel/syscall.h
@@ -20,3 +20,4 @@
 #define SYS_link   19
 #define SYS_mkdir  20
 #define SYS_close  21
+#define SYS_settickets 22
diff --git a/kernel/sysproc.c b/kernel/sysproc.c
index 419e727..8f04f60 100644
--- a/kernel/sysproc.c
+++ b/kernel/sysproc.c
@@ -107,3 +107,17 @@ sys_uptime(void)
   release(&tickslock);
   return xticks;
 }
+uint64
+sys_settickets(void)
+{
+  int n;
+  argint(0, &n);
+  if(n <= 0)
+    return -1;
+
+  struct proc *p = myproc();
+  acquire(&p->lock);
+  p->tickets = n;
+  release(&p->lock);
+  return 0;
+}
diff --git a/user/test_scheduler.c b/user/test_scheduler.c
new file mode 100644
index 0000000..89c55a4
--- /dev/null
+++ b/user/test_scheduler.c
@@ -0,0 +1,23 @@
+#include "kernel/types.h"
+#include "user/user.h"
+
+int
+main(int argc, char *argv[])
+{
+  if (argc != 2) {
+    printf("Usage: test_scheduler <tickets>\n");
+    exit(1);
+  }
+
+  int tickets = atoi(argv[1]);
+  if (settickets(tickets) < 0) {
+    printf("Error: settickets failed. Tickets must be > 0\n");
+    exit(1);
+  }
+
+  // Spin in an infinite loop to keep the process alive
+  while (1) {
+  }
+
+  return 0;
+}
diff --git a/user/user.h b/user/user.h
index ac84de9..681b0b2 100644
--- a/user/user.h
+++ b/user/user.h
@@ -47,3 +47,4 @@ void printf(const char*, ...) __attribute__ ((format (printf, 1, 2)));
 // umalloc.c
 void* malloc(uint);
 void free(void*);
+int settickets(int);
diff --git a/user/usys.pl b/user/usys.pl
index c5d4c3a..c5e15b5 100755
--- a/user/usys.pl
+++ b/user/usys.pl
@@ -42,3 +42,5 @@ entry("getpid");
 entry("sbrk");
 entry("pause");
 entry("uptime");
+entry("settickets");
+
